## 点子

数据管理：核心在一个配置文件里，支持导入导出，支持用户手动编辑（注意提示解析问题）  ~/.config/keyhub/settings.json

groups - item（key\url\模型列表\官网\图标\备注、描述（擅长的能力

查看使用量？ 

可以在这里发送一个接口测试是否通？

可以设置为过期

多组
嵌套组
多模型
多api key
多url


首页：
密钥列表：
    搜索
    供应商过滤
    列表
        名称
        供应商
        密钥
        url
        状态
        操作按钮
仪表盘：
用量分析：

图标列表 - 官网地址 + icon.ico 直接根据其官网地址获取！（内置几个常用的）


整体布局：
1. 左侧是菜单栏，有展开/收起按钮，内容：应用名称+版本号，跟着下面是操作行（新建组等），下面是的所有区域是可以滚动的树状的分组列表，列表里每个item都有图标+名称+操作按钮（编辑、删除、新增，新增）。
2. 右侧顶部是可以拖动的区域，高度和macos的应用级操作按钮一样高，里面中间显示当前应用储存的密钥总数量。
3. 右侧顶部以外的区域是内容区，暂时为空。

